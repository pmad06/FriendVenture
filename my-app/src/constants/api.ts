/**
 * Backend API base URL.
 *
 * For physical device testing (Expo Go), replace with your computer's local IP.
 *   Windows : run `ipconfig`  → IPv4 Address  (e.g. 192.168.1.42)
 *   Mac/Linux: run `ifconfig` → inet on en0
 *
 * Example: export const API_BASE_URL = 'http://192.168.1.42:5000';
 *
 * Web / iOS Simulator / Android Emulator → 'http://localhost:5000' works fine.
 */
export const API_BASE_URL = 'http://localhost:5000';
